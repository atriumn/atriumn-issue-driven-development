name: CI/CD for Atriumn GitHub App

on:
  push:
    branches: [main, develop]
  pull_request:
    paths:
      - 'github-app/**'

jobs:
  test-and-deploy:
    runs-on: ubuntu-latest
    environment: ${{ github.ref == 'refs/heads/main' && 'production' || 'development' }}
    
    defaults:
      run:
        working-directory: ./github-app

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: github-app/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Run unit tests
        # Add the --coverage flag to generate a report
        run: npm test -- --coverage

      # Coverage enforcement is handled automatically by Jest via coverageThreshold in package.json
      # The test step above will fail if coverage is below 80% for any metric (statements, branches, functions, lines)

      # --- Vercel Deployment Steps ---
      - name: Install Vercel CLI
        run: npm install -g vercel

      - name: Pull Vercel Project Information
        env:
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
        run: vercel pull --yes --environment=${{ github.ref == 'refs/heads/main' && 'production' || 'preview' }} --token=${{ secrets.VERCEL_TOKEN }}
        
      - name: Build Project Artifacts
        env:
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
        run: vercel build ${{ github.ref == 'refs/heads/main' && '--prod' || '' }} --token=${{ secrets.VERCEL_TOKEN }}

      - name: Deploy Project Artifacts to Vercel
        run: vercel deploy --prebuilt --token=${{ secrets.VERCEL_TOKEN }} ${{ github.ref == 'refs/heads/main' && '--prod' || '' }}